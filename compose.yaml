services:
  web:
    image: un1ver5e/notes-example
    build:
      context: .
      dockerfile: Web/Dockerfile
    volumes: ["./.db:/db"]
    ports: ["8080:8080"]
    environment:
      ConnectionStrings__Sqlite: Data Source=/db/notes.db

